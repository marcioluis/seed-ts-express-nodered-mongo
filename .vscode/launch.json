{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "node",
			"request": "attach",
			"name": "Nodemon Attach",
			"cwd": "${workspaceFolder}",
			"processId": "${command:PickProcess}",
			"restart": true,
			"protocol": "inspector",
			"port": 9229
		}
	]
}
